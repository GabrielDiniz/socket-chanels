

services:
  versa-atende-painel-server:
    # Hot-reload + modo dev
    command: npm run dev:api:debug
    environment:
      - NODE_ENV=development
      - NEXT_ENABLED=false
    volumes:
      - .:/app:delegated
      - /app/node_modules
    ports:
      - "3000:3000"
      - "24678:24678"             # opcional: VSCode debugger

  versa-atende-painel-test:
    container_name: versa-atende-painel-test
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    command: npm run test:broadcast
    environment:
      - SERVER_URL=http://versa-atende-painel-server:3000   # ← como você pediu
      - CHANNEL=recepcao_01
    depends_on:
      versa-atende-painel-server:
        condition: service_healthy
    networks:
      - app-network
    volumes:
      - .:/app:cached
      - /app/node_modules
      - /app/dist-test
  migrate:
    entrypoint: ["npx", "prisma", "migrate", "dev"]
    # ← entrypoint fixo, argumentos livres
    command: []
    tty: true                           # ← ESSENCIAL pro modo interativo
    stdin_open: true
    volumes:
      - .:/app:cached
      - /app/node_modules