// src/setupTests.ts — Setup pra frontend project (jest-dom extends, polyfills leves se precisar — jsdom já tem localStorage/document)

import '@testing-library/jest-dom';

// Opcional: polyfills extras se algum hook usar globals raros (não precisamos agora)